@model IEnumerable<BankData.Data.Bank.Koszyk>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdNagroda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ilosc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdNagroda)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ilosc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKoszyk">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdKoszyk">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdKoszyk">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
